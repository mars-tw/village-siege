name: village-siege-production

services:
  caddy:
    image: caddy:2.11.4-alpine@sha256:5f5c8640aae01df9654968d946d8f1a56c497f1dd5c5cda4cf95ab7c14d58648
    restart: unless-stopped
    init: true
    depends_on:
      client:
        condition: service_healthy
        restart: true
      server:
        condition: service_healthy
        restart: true
    environment:
      ACME_EMAIL: ${ACME_EMAIL:?Set the ACME account email}
      PUBLIC_GAME_HOST: ${PUBLIC_GAME_HOST:?Set the public game hostname without a scheme}
      PUBLIC_SERVER_HOST: ${PUBLIC_SERVER_HOST:?Set the public server hostname without a scheme}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - edge
      - application
    read_only: true
    tmpfs:
      - /tmp:size=32m,mode=1777
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    stop_signal: SIGTERM
    stop_grace_period: 15s
    healthcheck:
      test:
        - CMD-SHELL
        - wget --quiet --output-document=- http://127.0.0.1:2019/healthz | grep -qx OK
      start_period: 10s
      interval: 30s
      timeout: 5s
      retries: 3

  client:
    image: ${VILLAGE_SIEGE_CLIENT_IMAGE:-ghcr.io/mars-tw/village-siege-client}:${VILLAGE_SIEGE_TAG:-0.19.0}
    build:
      context: ..
      dockerfile: apps/client/Dockerfile
      args:
        NODE_VERSION: ${NODE_VERSION:-24.15.0}
        NODE_IMAGE_DIGEST: ${NODE_IMAGE_DIGEST:-sha256:4e6b70dd6cbfc88c8157ba19aa3d9f9cce6ba4703576d55459e45efcbc9c5f5d}
        VITE_MULTIPLAYER_ENABLED: "false"
        VITE_BASE_URL: /
    restart: unless-stopped
    init: true
    depends_on:
      server:
        condition: service_healthy
        restart: true
    environment:
      PORT: "8080"
      PUBLIC_CONNECT_ORIGIN: https://${PUBLIC_SERVER_HOST:?Set the public server hostname without a scheme}
    expose:
      - "8080"
    networks:
      - application
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    stop_signal: SIGTERM
    stop_grace_period: 15s

  server:
    image: ${VILLAGE_SIEGE_SERVER_IMAGE:-ghcr.io/mars-tw/village-siege-server}:${VILLAGE_SIEGE_TAG:-0.19.0}
    build:
      context: ..
      dockerfile: Dockerfile
      args:
        NODE_VERSION: ${NODE_VERSION:-24.15.0}
        NODE_IMAGE_DIGEST: ${NODE_IMAGE_DIGEST:-sha256:4e6b70dd6cbfc88c8157ba19aa3d9f9cce6ba4703576d55459e45efcbc9c5f5d}
    restart: unless-stopped
    init: true
    depends_on:
      redis:
        condition: service_healthy
        restart: true
      postgres:
        condition: service_healthy
        restart: true
    environment:
      NODE_ENV: production
      PORT: "2567"
      GIT_COMMIT_SHA: ${GIT_COMMIT_SHA:?Set the deployed Git commit SHA}
      ALLOWED_ORIGINS: https://${PUBLIC_GAME_HOST:?Set the public game hostname without a scheme}
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_PASSWORD_FILE: /run/secrets/redis_password
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432"
      POSTGRES_DB: ${POSTGRES_DB:-village_siege}
      POSTGRES_USER: ${POSTGRES_USER:-village_siege}
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
    expose:
      - "2567"
    secrets:
      - redis_password
      - postgres_password
    networks:
      - application
      - data
    read_only: true
    tmpfs:
      - /tmp:size=64m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    stop_signal: SIGTERM
    stop_grace_period: 30s
    deploy:
      replicas: 1
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "fetch('http://127.0.0.1:2567/health/ready').then(r=>{if(!r.ok)throw new Error(String(r.status))}).catch(()=>process.exit(1))"
      interval: 10s
      timeout: 3s
      start_period: 15s
      retries: 5

  redis:
    image: redis:7.4-alpine@sha256:6ab0b6e7381779332f97b8ca76193e45b0756f38d4c0dcda72dbb3c32061ab99
    restart: unless-stopped
    user: redis
    command:
      - /bin/sh
      - -ec
      - >-
        REDIS_PASSWORD="$$(cat /run/secrets/redis_password)";
        REDIS_PASSWORD_LENGTH="$${#REDIS_PASSWORD}";
        if [ "$${REDIS_PASSWORD_LENGTH}" -lt 32 ] || [ "$${REDIS_PASSWORD_LENGTH}" -gt 128 ]; then
          echo "Redis password must contain 32 to 128 base64url characters" >&2;
          exit 1;
        fi;
        case "$${REDIS_PASSWORD}" in
          *[!A-Za-z0-9_-]*)
            echo "Redis password must contain only base64url characters" >&2;
            exit 1;;
        esac;
        umask 077;
        {
          printf '%s\n' 'appendonly yes';
          printf '%s\n' 'appendfsync everysec';
          printf '%s\n' 'save 60 1';
          printf 'requirepass %s\n' "$${REDIS_PASSWORD}";
        } > /tmp/redis.conf;
        unset REDIS_PASSWORD REDIS_PASSWORD_LENGTH;
        exec redis-server /tmp/redis.conf
    expose:
      - "6379"
    secrets:
      - redis_password
    volumes:
      - redis-data:/data
    networks:
      - data
    read_only: true
    tmpfs:
      - /tmp:size=16m,mode=1777
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    stop_signal: SIGTERM
    stop_grace_period: 30s
    healthcheck:
      test:
        - CMD-SHELL
        - REDISCLI_AUTH="$$(cat /run/secrets/redis_password)" redis-cli ping | grep -qx PONG
      interval: 5s
      timeout: 3s
      retries: 10

  postgres:
    image: postgres:17-alpine@sha256:742f40ea20b9ff2ff31db5458d127452988a2164df9e17441e191f3b72252193
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-village_siege}
      POSTGRES_USER: ${POSTGRES_USER:-village_siege}
      POSTGRES_PASSWORD_FILE: /run/secrets/postgres_password
    expose:
      - "5432"
    secrets:
      - postgres_password
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - data
    shm_size: 128mb
    security_opt:
      - no-new-privileges:true
    stop_signal: SIGTERM
    stop_grace_period: 30s
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U "$${POSTGRES_USER}" -d "$${POSTGRES_DB}"
      interval: 5s
      timeout: 3s
      retries: 10

networks:
  edge:
  application:
    internal: true
  data:
    internal: true

volumes:
  caddy-data:
  caddy-config:
  redis-data:
  postgres-data:

secrets:
  redis_password:
    file: ${COMPOSE_REDIS_SECRET_FILE:?Run through deploy/production-compose.sh}
  postgres_password:
    file: ${COMPOSE_POSTGRES_SECRET_FILE:?Run through deploy/production-compose.sh}
